<%= render partial: 'form', locals: {
                            path: "/applications/#{@application.id}",
                            method: :post,
                            button_text: "submit"
                                    }%>
